namespace Conversor.Xml
{
    public class XFlutterContext : XContext
    {
        public override void ProcessWindowNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessUserControlNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessListBoxNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessComboBoxNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessLabelNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessButtonBaseNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessButtonNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessTextBoxBaseNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessTextBoxNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessPanelNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessGridNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessPageNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessImageNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessSelectorNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessItemsControlNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessControlNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }

        public override void ProcessContentControlNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }


        public override void ProcessStatusBarItemNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessScrollViewerNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessGroupItemNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessListBoxItemNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessComboBoxItemNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessListViewItemNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessRepeatButtonNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessToggleButtonNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessCheckBoxNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessRadioButtonNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessGridViewColumnHeaderNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessExpanderNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessGroupBoxNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessTabItemNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessListViewNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessTabControlNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessMultiSelectorNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessTreeViewItemNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessToolBarNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }

        public override void ProcessMenuItemNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;
            
            // Do something
        }



        public override void ProcessDefaultNode(XXmlNode node)
        {
            var attributes = node.Attributes;
            var children = node.ChildNodes;

            // Do something
        }
    }
}